export const stringArrayType = {
    items: {
        type: 'string',
    },
    type: 'array',
} as const;

export const stringType = {
    type: 'string',
} as const;

export const objectType = {
    type: 'object',
} as const;
