<div class="container pt-3">
    <h1>Badges</h1>
    <p>Badges are decorations or awards given to a user, a user can have multiple badges at a time.</p>

    <div class="table-responsive">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">Name</th>
                <th scope="col"><i class="fa-brands fa-teamspeak fa-fw"></i>Teamspeak group</th>
                <th scope="col"><i class="fa-brands fa-discord fa-fw"></i>Discord role</th>
                {{#hasPermission user 'EDIT_BADGES'}}
                    <th scope="col" class="text-right">Actions</th>
                {{/hasPermission}}
            </tr>
            </thead>
            <tbody>
            {{#each badges}}
                <tr>
                    <th class="align-middle">{{this.name}}</th>
                    <td class="align-middle">
                        {{#if this.teamspeakRank}}
                            {{this.teamspeakRank.name}} ({{this.teamspeakRank.id}})
                        {{else}}
                            None
                        {{/if}}
                    </td>
                    <td class="align-middle">
                        {{#if this.discordRank}}
                            ... (#)
                        {{else}}
                            None
                        {{/if}}
                    </td>
                    {{#hasPermission ../user 'EDIT_BADGES'}}
                        <td class="text-right align-middle">
                            <a href="/badges/edit/{{../this.id}}" class="btn btn-warning">
                                <i class="fa-solid fa-pen fa-fw"></i>
                            </a>
                            <a href="/badges/delete/{{../this.id}}" class="btn btn-danger">
                                <i class="fa-solid fa-trash fa-fw"></i>
                            </a>
                        </td>
                    {{/hasPermission}}
                </tr>
            {{/each}}
            </tbody>
        </table>
    </div>

    {{#hasPermission user 'EDIT_BADGES'}}
        <a href="/badges/create" class="btn btn-primary">Create badge</a>
    {{/hasPermission}}

</div>
